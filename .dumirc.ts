import { defineConfig } from 'dumi';

export default defineConfig({
  themeConfig: {
    name: '瓢儿白',
    logo: '/logo.jpeg',
    nav: [
      {
        title: 'es6',
        link: '/es6/let-const',
      },
    ],
  },
});
